<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%=  @cocktail.image %>);">
  <div class="container">
    <h1 class="cocktailheadline"><%=  @cocktail.name %></h1>
  </div>
</div>


<% @cocktail.doses.each do |dose|%>
<%= dose.description  %>
<%= dose.ingredient.name %>
<%= link_to "❌", dose_path(dose), method: :delete %>

<% end %>

<%= render 'doses/form' %>



<%= link_to "Go back to the overview", :action => "index" %>

